#include <bits/stdc++.h>
using namespace std;
int main()
{
    string st = "RakibulIslam";

    string::iterator it;
    for (it = st.begin(); it != st.end(); it++)
    {
        cout << *it << endl;
    }
}